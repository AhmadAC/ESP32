# Android Studio
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
/0
# Secrets (if any)
*.keystore
*.jks
google-services.json

# Generated Gradle wrapper files (if you decide to commit them locally later)
# For this "fully online" setup, you are NOT committing these, so they wouldn't be ignored from the repo
# But if you run 'gradle wrapper' locally, these lines would prevent committing them.
# gradlew
# gradlew.bat
# gradle/wrapper/gradle-wrapper.jar
# gradle/wrapper/gradle-wrapper.properties # If generated locally and not the one you want
